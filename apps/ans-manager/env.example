VITE_ARCH_RPC_URL=https://rpc.testnet.arch.network
VITE_ANS_NETWORK=testnet
# Turnkey Wallet Hub is not wired in this static SPA yet (needs a same-origin
# proxy for CORS / API key). LaserEyes + Arch Wallet cover mutations today.
# VITE_WALLET_HUB_BASE_URL=
