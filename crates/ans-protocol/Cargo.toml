[package]
name = "ans-protocol"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Deterministic codecs and reference resolution for Arch Name Service"

[dependencies]
arch_program = "=0.6.2"
bitcoin = "0.32.5"
borsh = { version = "=1.5.3", features = ["derive"] }
sha2 = "0.10.8"
thiserror = "2.0.12"
